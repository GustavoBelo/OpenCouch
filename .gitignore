# Build artifacts
app/build/
app-build/
app/obj-*/
appimage-build/
appimage-dist/
*.AppImage
build/
*.o
*.obj
*.qm

# Debian packaging build output (the .deb/.changes/.buildinfo land in the parent
# directory, outside the repo)
/debian/.debhelper/
/debian/open-couch/
/debian/open-couch-engine/
/debian/files
/debian/debhelper-build-stamp
/debian/*.substvars
/debian/*.debhelper.log

# Python (local test tooling only; the shipped app is C++/QML)
.venv/
__pycache__/
*.pyc

# Flatpak build/state
.flatpak-builder/
flatpak-build/
flatpak-repo/
repo/
*.flatpak

build-fedora/
test-appdir/

# IDE / editor
.vscode/
.idea/
*.kdev4
.kdev4/

# OS
.DS_Store

# Local context notes and the bats download directory from an abandoned branch.
/context.md
/tests/
