#!/usr/bin/env bash
source "$(dirname "${BASH_SOURCE[0]}")/_shim.bash"
oc_record "wmctrl $*"
if [[ "${1:-}" == "-lx" ]]; then
    [[ -n "${OC_WMCTRL_LIST:-}" && -f "${OC_WMCTRL_LIST}" ]] && cat "${OC_WMCTRL_LIST}"
    exit 0
fi
exit "${OC_WMCTRL_RC:-0}"
